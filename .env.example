# Redis Configuration
# Address of your external Redis server (host:port)
REDIS_ADDR=localhost:6379

# Redis password (leave empty if no password is required)
REDIS_PASSWORD=

# Redis database number (default is 0)
REDIS_DB=0

# Port for the web server (default is 8080)
PORT=8080
